import { Meta, Story, Canvas, Preview, Source } from '@storybook/addon-docs'

import Sticker from './index.tsx'
import { Colors } from '../../../commons'
import dedent from 'ts-dedent'

<Meta title='Atoms/Sticker' component={Sticker} />

export const Template = (args) => <Sticker {...args} />

# Sticker

Un pequeño "cuadro" de texto para resaltar alguna información enfatizada.

## Uso del componente

<Source
  language='typescript'
  code={dedent`
    <Sticker text="TXT" variant={Color.SUCCESS_LIGHT} />
  `}
/>
Resultado: 🤗{' '}

<Sticker variant={Colors.SUCCESS_LIGHT} />

## **Variantes**

Existen colores establecidos para poder usar a conveniencia. Todos colores claros asociados a la paleta de colores de la app.

<Canvas>
  <Story name='Default'>
    <Sticker text='Default' />
  </Story>
  <Story name='Warning'>
    <Sticker text='Warning' variant={Colors.WARNING} />
  </Story>
  <Story name='Primary'>
    <Sticker text='Primary' variant={Colors.PRIMARY} />
  </Story>
  <Story name='Secondary'>
    <Sticker text='Success' variant={Colors.SUCCESS} />
  </Story>
  {/*  */}
  <Story name='Warning Light'>
    <Sticker text='Warning Light' variant={Colors.WARNING_LIGHT} />
  </Story>
  <Story name='Primary Light'>
    <Sticker text='Primary Light' variant={Colors.PRIMARY_LIGHT} />
  </Story>
  <Story name='Success Light'>
    <Sticker text='Success Light' variant={Colors.SUCCESS_LIGHT} />
  </Story>
</Canvas>

## Detalles Técnicos

**interfaz** del componente.

```ts
interface StickerProps {
  variant: Colors
  shadow?: boolean
  border?: boolean
  shadowDirection?: 'left' | 'right'
}
```

**enum** de colores.

```ts
enum Colors {
  PRIMARY = 'primary',
  DEFAULT = 'default',
  SUCCESS = 'success',
  WARNING = 'warning',
  SUCCESS_LIGHT = 'success-light',
  PRIMARY_LIGHT = 'primary-light',
  WARNING_LIGHT = 'warning-light'
}
```
