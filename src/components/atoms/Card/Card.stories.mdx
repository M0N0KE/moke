import { Meta, Story, Canvas, Preview, Source } from '@storybook/addon-docs'

import Card from './index.tsx'
import { Colors } from '../../../commons'
import dedent from 'ts-dedent'

<Meta title='Atoms/Card' component={Card} />

export const Template = (args) => (
  <Card {...args}>
    Un pequeño cuadro de texto con un fondo oscuro para mostrar etiquetas o
    información enfatizada en un área condensada.
  </Card>
)

# Card

Una tarjeta para presentar de diferentes formas diferentes informaciones.

## Uso del componente

<Source
  language='typescript'
  code={dedent`
    <Card variant={ButtonColors.DEFAULT} >
      Una tarjeta para presentar de diferentes formas diferentes informaciones.
    </Card>
  `}
/>
Resultado: 🤗{' '}

<Story
  name='Basic'
  args={{
    variant: 'default',
    shadow: false
  }}
>
  {Template.bind({})}
</Story>

## Variantes

las variantes no solo son de colores, si no tambien sombra. La cual podrás indicar al componete si apunta a la derecha o a la izquierda.

<Canvas>
  <Story name='Default'>
    <Card shadow={false}>Sin Shadow</Card>
  </Story>
  <Story name='Shadow'>
    <Card shadow={true}>Con Shadow</Card>
  </Story>
  <Story name='Shadow Left'>
    <Card shadow={true} ShadowDirection='left'>
      Con Shadow izquierda
    </Card>
  </Story>
  <Story name='Shadow Right'>
    <Card shadow={true} shadowDirection='right'>
      Con Shadow derecha
    </Card>
  </Story>
</Canvas>

## Colores

<Canvas>
  <Story name='Primary'>
    <Card variant={Colors.PRIMARY} shadow={false}>
      Sin Shadow
    </Card>
  </Story>
  <Story name='Success'>
    <Card variant={Colors.SUCCESS} shadow={true}>
      Con Shadow
    </Card>
  </Story>
  <Story name='Success Light'>
    <Card variant={Colors.SUCCESS_LIGHT} shadow={true} ShadowDirection='left'>
      Con Shadow izquierda
    </Card>
  </Story>
  <Story name='Warning Light'>
    <Card variant={Colors.WARNING_LIGHT} shadow={true} shadowDirection='right'>
      Con Shadow derecha
    </Card>
  </Story>
</Canvas>

## Detalles Técnicos

Interfaz del componente.

```ts
interface CardProps {
  variant: Colors
  shadow?: boolean
  shadowDirection?: 'left' | 'right'
  border?: boolean
  children: any
}
```

enum de colores.

```ts
enum Colors {
  PRIMARY = 'primary',
  DEFAULT = 'default',
  SUCCESS = 'success',
  WARNING = 'warning',
  SUCCESS_LIGHT = 'success-light',
  PRIMARY_LIGHT = 'primary-light',
  WARNING_LIGHT = 'warning-light'
}
```
